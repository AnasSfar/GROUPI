# En dev, laisser vide/absent : Vite proxy déjà /api/v1 vers l'API locale (voir vite.config.ts).
# En prod (Vercel) : URL absolue de l'API déployée, avec le préfixe /api/v1.
# VITE_API_BASE_URL=https://groupi-api.vercel.app/api/v1

# Optionnel — sans VITE_SENTRY_DSN, aucune remontée d'erreur, comportement inchangé.
# VITE_SENTRY_DSN=
